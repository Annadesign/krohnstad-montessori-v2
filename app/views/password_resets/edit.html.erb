<div class="avdeling-blue" style="padding:80px 0;">
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <%= form_for @parent, :html=> {:id => 'form-parent', :url => password_reset_path(params[:id])} do |f| %>
          <% if @parent.errors.any? %>
            <div class="error_messages">
              <h2>Form is invalid</h2>
              <ul>
                <% for message in @parent.errors.full_messages %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="field">
            <%= f.label :password, 'Nytt passord' %>
            <%= f.password_field :password, {:class => 'form-control', :placeholder => 'Passord', 'data-bv-notempty' => 'true', 'data-bv-notempty-message' =>'Passord kreves og kan ikke være tom','data-bv-identical' => 'true', 'data-bv-identical-field' => 'password_confirmation', 'data-bv-identical-message' => 'Passord er ikke det samme i begge felt' } %>
          </div>
          <div class="field">
            <%= f.label :password_confirmation, 'Gjenta passord' %>
            <%= f.password_field :password_confirmation, {:class => 'form-control', :placeholder => 'Passord', 'data-bv-notempty' => 'true', 'data-bv-notempty-message' =>'Passord kreves og kan ikke være tom','data-bv-identical' => 'true', 'data-bv-identical-field' => 'password', 'data-bv-identical-message' => 'Passord er ikke det samme i begge felt' }  %>
          </div>
          <div class="actions">
            <%= f.submit "Gjenopprett passord" %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript">
  $(document).ready(function() {
      $('#form-parent').bootstrapValidator();
  });
</script>